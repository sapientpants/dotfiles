case $1 in
	start)
		docker_del_stopped qdrant

		docker run -d \
			--name qdrant \
			-p 6333:6333 \
			qdrant/qdrant:latest
		;;
	stop)
		docker container stop qdrant
		;;
	*)
		echo "Usage: $0 {start|stop}"
		return 1
		;;
esac
